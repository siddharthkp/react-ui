{
  "name": "react-ui-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/ds-tools/*",
    "packages/ecosystem/*"
  ],
  "scripts": {
    "test": "cd packages/react-ui && yarn test"
  }
}
