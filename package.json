{
  "name": "react-ui-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/ds-tools/*"
  ],
  "scripts": {
    "test": "cd packages/react-ui && yarn test"
  }
}
